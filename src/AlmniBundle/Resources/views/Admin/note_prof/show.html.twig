{% extends '@Almni/Admin/base.html.twig' %}


{% block body %}
    <h1>Note</h1>
    <div class="row">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Note N° {{note.id }} </h4>

                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                            <tr>
                                <th>#</th>
                                <th>Valeur</th>

                            </tr>
                            </thead>
                            <tbody>
                            <tr>
                                <td>ID</td>

                                <td><span class="label label-warning">{{note.id }}</span></td>


                            </tr>
                            <tr>
                                <td>Matiere</td>
                                <td>{{ note.idmatiere }}</td>

                            </tr>
                            <tr>
                                <td>Formule</td>
                                <td>{{ note.formule }}</td>

                            </tr>
                            <tr>
                                <td>Note</td>

                                <td><span class="label label-danger">{{ note.note }}</span></td>

                            </tr>
                            <tr>
                                <td>Remarque</td>
                                <td>{{note.remarque }}</td>

                            </tr>
                            </tbody>
                        </table>


                        <a href="{{ path('note_index') }}" ><button type="button" class="btn waves-effect waves-light btn-rounded btn-danger">List des Notes</button></a>

                        <a href="{{ path('note_edit', { 'id': note.id }) }}" ><button type="button" class="btn waves-effect waves-light btn-rounded btn-warning">Modifier</button></a>

                    </div>

                </div>
            </div>
        </div>
    </div>


{% endblock %}
